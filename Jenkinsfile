@Library('robo-shared-library@main') _   // should deleclare to use shred lib and the branch agau=ist which its running....

env.COMPONENT="cart"
env.APP_TYPE="nodejs"

nodejs()





//@Library('robo-shared-library@main') _   // should deleclare to use shred lib and the branch agau=ist which its running

//pipeline {
//    agent any
//    stages{
//        // should run on every coomit to feautre branch 
//        stage('Lint Check'){
//            steps{
//                 script {                     // all fuctions called from groovy file should be placed inside scrpts
//                sample.info("Hello")          // sample is the file name of the groovy inside shared lib and info is the fuction name that defined 
//
//            }
//            sh "echo Installing Jslint"
//           sh "npm install jslint"
//            sh "ls -ltr node_modules/jslint/bin"
//            sh "~/node_modules/jslint/bin/jslint.js server.js || true"   // true is used to mark the present step as pass and skip to next step  even if lint check fails  
//
//        }
//    }
//}

//}
